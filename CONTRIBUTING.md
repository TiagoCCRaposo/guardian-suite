# Contribuir para o Guardian Suite

Obrigado pelo teu interesse em contribuir para o Guardian Suite! 🎉

## Como Contribuir

1.  **Faz um Fork** do repositório.
2.  Cria uma **Branch** para a tua funcionalidade (`git checkout -b feature/nova-funcionalidade`) ou correção de bug (`git checkout -b fix/bug-bizarro`).
3.  Faz os teus **Commits** seguindo boas práticas (mensagens claras).
4.  Faz **Push** para a branch (`git push origin feature/nova-funcionalidade`).
5.  Abre um **Pull Request** (PR) para o repositório original.

## Regras de Desenvolvimento

* **Estilo de Código:** Mantém a consistência com o código existente (o projeto usa ESLint e Prettier).
* **Testes:** Se adicionares uma nova funcionalidade, tenta incluir testes relevantes.
* **Commits:** Tenta usar [Conventional Commits](https://www.conventionalcommits.org/) (ex: `feat: adiciona monitorização de disco` ou `fix: corrige erro na api`).

## Dúvidas?

Se tiveres dúvidas, sente-te à vontade para abrir uma *Issue* marcada como "question" ou "help wanted".
